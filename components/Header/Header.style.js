import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  image: {
    width: 170,
    // objectFit:"contain"
  },
  header: {
    alignItems: "center",
  },
    subtitel: {
        fontSize: 20,
        color: "#ababab",
        marginTop: -20,
    },
});
